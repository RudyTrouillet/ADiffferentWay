{# {% extends 'base.html.twig' %} #}


{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <body>
        <nav>  
        {# <img src="{{ asset('images/logo.jpg') }}" alt="logo" class="w-25 rounded mx-auto d-block">    #}
        <a href="{{ path('app_home') }}">Contact</a>
        <a href="{{ path('app_home') }}">NewsLetter</a>
        {# <a href="{{ path('app_home') }}">InterViews</a>
        <a href="{{ path('app_home') }}">Concerts</a>
        <a href="{{ path('app_home') }}">Media Player</a>    #}
        </nav>

        
    </body>
{% endblock %}
